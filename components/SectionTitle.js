import styled from "styled-components";
import {animated} from "react-spring";

const SectionTitle_SC = animated(styled.p`
    font-size: 30px;
    font-style: normal;
    font-weight: bold;
    letter-spacing: 0.2em;
    margin: 0 0 80px 0;
`);

export default SectionTitle_SC;